version: 2.1
jobs:
  build:
    docker:
      - image: saadqamar/circleci_ali_dev_secops_image
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Unit Tests
          command: npm test
  
  docker:
    docker:
      - image: docker:19.03.12
    steps:
      - setup_remote_docker:
          version: 19.03.12
      - checkout
      - run:
          name: Build Docker Image
          command: |
            docker build -t your-dockerhub-username/circleci-devsecops-image .
      - run:
          name: Push Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            docker push your-dockerhub-username/circleci-devsecops-image
workflows:
  version: 2
  build_and_push:
    jobs:
      - build
      - docker:
          requires:
            - build

